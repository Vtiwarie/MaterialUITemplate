<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <!-- TODO: define optional toolbar themes -->
    <style name="Theme.Toolbar" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar"></style>
    <style name="Theme.ToolbarOverlay" parent="@style/ThemeOverlay.AppCompat.Light"></style>
    <style name="Theme.ToolbarText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title"></style>
    <style name="Theme.AppBarLayout"  parent="ThemeOverlay.AppCompat.Dark.ActionBar"></style>

    <!-- tab layout styles -->
    <style name="TabLayout" parent="Base.Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/tabIndicatorColor</item>
        <item name="tabIndicatorHeight">4dp</item>
    </style>
</resources>
